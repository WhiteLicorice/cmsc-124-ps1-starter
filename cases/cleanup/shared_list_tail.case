# b and a share every cell of b. Freeing must free each cell once, which is why
# dt_list_free frees one cell and never follows the tail.
list nil e
list cons c 3 e
list cons b 2 c
list cons a 1 b
list cdr a from_a
list len from_a
list len b
print @b
print @a
